---

- name: Install apps
  hosts: home_machines
  become: yes
  become_method: sudo
  vars_files:
    - ./vars/config.yml
    - .config.yml

  roles:
    - basic/user
    # - raspbian/docker
    # - raspbian/nfs
    # - raspbian/samba
    - sharing/nfs
    - sharing/samba
    - sharing/vnc
